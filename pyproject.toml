[project]
name = "maul"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyterlab>=4.3.0",
    "pandas>=2.2.3",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.7.3",
]
